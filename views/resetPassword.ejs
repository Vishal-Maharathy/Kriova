<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>
    <title>Reset Password</title>
</head>
<body>
    <img src="/imgs/bg1.jpg" class="backgroundImage" alt="image">
    <h1 class="title">Kriova SignIn</h1>
    <form action="/redirectUpdatePass/<%=token%>" method="POST" class="form">
        <input type="password" name="password" placeholder="Enter your Password" required></br>
        <input type="password" name="cpassword" placeholder="Enter Password Again" required></br>
        <button type="submit">Update Password</button>
    </form>
</body>
<script>
    <%if(flash.error && flash.error.length>0){%>
        new Noty({
            text: '<%=flash.error%>',
            theme: 'sunset',
            type: 'error',
            layout: 'topRight',
            timeout: 1500,
        }).show();
    <%}%>
    <%if(flash.success && flash.success.length>0){%>
        new Noty({
            text: '<%=flash.error%>',
            theme: 'sunset',
            type: 'success',
            layout: 'topRight',
            timeout: 1500,
        }).show();
    <%}%>
</script>
</html>